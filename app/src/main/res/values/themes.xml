<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PDA" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/RojoFuerte</item>
        <item name="colorPrimaryVariant">@color/RojoFuerte</item>
        <item name="colorOnPrimary">@color/white</item>
    </style>

    <style name="Theme.PDA" parent="Base.Theme.PDA" />

    <style name="Theme.PDA.Fullscreen" parent="Theme.PDA">
        <item name="android:actionBarStyle">@style/Widget.Theme.PDA.ActionBar.Fullscreen</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
    </style>

    <style name="ThemeOverlay.PDA.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/light_blue_600</item>
        <item name="fullscreenTextColor">@color/light_blue_A200</item>
    </style>
</resources>